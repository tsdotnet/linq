/**
 * Returns the sum (.value) of a sequence and the number of entries (.count).
 * @param {Iterable} sequence The sequence of numbers to sum and count.
 * @return The result.
 */
export declare function sumAndCount(sequence: Iterable<number>): {
    value: number;
    count: number;
};
